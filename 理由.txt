エラーの理由

・masterブランチのリモートとローカルの最新情報が異なっていたため。

対処法として
次新しく追加・更新するメンバーは、次のコマンドを打つ。
１．git fetch リモートレポジトリのコミット履歴をとってきて、ローカルレポジトリにコピーする
２．git pull origin master リモートから取ってくる
３．git push origin master リモートに上げる
　上記を行うことで上げられることが分かった。